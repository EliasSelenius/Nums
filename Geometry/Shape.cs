


using Nums.Vectors;

namespace Nums.Geometry {
    /*public class Shape<T> : IShape where T : struct, IShape {

        T Value;

        public Shape(T shape) {
            Value = shape;
        }


        public static implicit operator T(Shape<T> a) => a.Value;
        public static implicit operator Shape<T>(T a) => new Shape<T>(a);

        #region IShape impl
        float IShape.Volume { get => Value.Volume; set => Value.Volume = value; }
        float IShape.Surface { get => Value.Surface; set => Value.Surface = value; }

        float IShape.FurthestLengthFromPivot() => Value.FurthestLengthFromPivot();

        Vec3 IShape.RandomPoint() => Value.RandomPoint();

        Vec3 IShape.RandomSurfacePoint() => Value.RandomSurfacePoint();

        void IShape.Scale(float scaler) => Value.Scale(scaler);

        #endregion
    }*/
}